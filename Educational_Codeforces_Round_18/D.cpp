#include <bits/stdc++.h> 
using namespace std;
#define endl "\n"
#define ll long long

map<ll, ll> pa;
int main() {
    ll n, q;
    cin >> n >> q;

    ll m = (n+1)/2;
    pa[m] = m;
    while (q--) {
        ll u, e, d;
        e = m;
        d = n;
        string s;
        cin >> u >> s;
        ll a = m;
        
        while (a != u) {
            
        }

    }

    return 0;  
}
